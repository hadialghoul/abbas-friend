# Copy this file to .env and fill in your values
# Get-Content .env.example | Set-Content .env  (PowerShell)
# cp .env.example .env  (Mac/Linux)

PORT=3000

# Gmail / Google Workspace - use App Password (not regular password)
# Enable 2FA first, then create at: https://myaccount.google.com/apppasswords
EMAIL_USER=sam@swap-remodeling.com
EMAIL_PASSWORD=your-app-password
